{
	"name": "backend-for-frontend",
	"version": "1.0.0",
	"description": "BFF em Node para atender algumas demanadas solicitadas pelo teste.",
	"main": "dist/server.js",
	"scripts": {
		"tsc": "tsc",
		"dev": "set debug=* && ts-node-dev --inspect --respawn --transpile-only ./src/server.ts",
		"start": "tsc && node dist/server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Lucas Simões",
	"license": "ISC",
	"devDependencies": {
		"@types/cors": "^2.8.9",
		"@types/express": "^4.17.11",
		"tslint": "^6.1.3",
		"typescript": "^4.1.5"
	},
	"dependencies": {
		"axios": "^0.21.1",
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"firebase": "^8.2.7",
		"firebase-admin": "^9.5.0",
		"helmet": "^4.4.1",
		"ts-node-dev": "^1.1.1"
	}
}
